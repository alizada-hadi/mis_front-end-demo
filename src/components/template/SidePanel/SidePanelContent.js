import React from "react";
import ThemeConfigurator from "../../template/ThemeConfigurator";
const SidePanelContent = (props) => {
  return <ThemeConfigurator {...props} />;
};

export default SidePanelContent;
